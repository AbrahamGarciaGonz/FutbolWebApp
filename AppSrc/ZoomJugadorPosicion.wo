// C:\DataFlex Projects\FutbolWebApp\AppSrc\ZoomJugadorPosicion.wo
// ZoomJugadorPosicion
//

Use cWebView.pkg
Use cWebPanel.pkg
Use cWebMenuGroup.pkg
Use cWebMenuItem.pkg
Use cWebForm.pkg

Use cligaDataDictionary.dd
Use cequipoDataDictionary.dd
Use cjugadorDataDictionary.dd
Use cposicionDataDictionary.dd
Use cjugadorposicionDataDictionary.dd
Use cWebGroup.pkg

Object oZoomJugadorPosicion is a cWebView
    Set piColumnCount to 12
    Set psCaption to "Posición del jugador"
    Set peWebViewStyle to wvsDrilldown
    Set peViewType to vtZoom
    Set pbShowCaption to False
    Set Verify_Save_Msg to 0
    Set piMaxWidth to 1024

    Object oLiga_DD is a cligaDataDictionary
    End_Object 

    Object oEquipo_DD is a cequipoDataDictionary
        Set DDO_Server to oLiga_DD
    End_Object 

    Object oJugador_DD is a cjugadorDataDictionary
        Set DDO_Server to oEquipo_DD
    End_Object 

    Object oPosicion_DD is a cposicionDataDictionary
    End_Object 

    Object oJugadorPosicion_DD is a cjugadorposicionDataDictionary
        Set DDO_Server To oJugador_DD
        Set DDO_Server To oPosicion_DD
    End_Object 

    Set Main_DD To oJugadorPosicion_DD
    Set Server  to oJugadorPosicion_DD

    Object oWebMainPanel is a cWebPanel
        Set piColumnCount to 12

        Object oDetallesGroup is a cWebGroup
            Set psCaption to "Detalles"
            Set piColumnCount to 12   
            Set pbShowCaption to False

            Object oJugadorPosicionID is a cWebForm
                Entry_Item jugadorposicion.ID
                Set piColumnIndex to 0
                Set peLabelPosition to lpTop
                Set psLabel to "ID"
                Set psBackgroundColor to "#e8ebeb"
            End_Object 

            Object oJugadorID is a cWebForm
                Entry_Item jugador.ID
                Set piColumnIndex to 0
                Set peLabelPosition to lpTop
                Set psLabel to "Jugador"
                Set pbPromptButton to True
    
                WebRegisterPath ntNavigateForward oSelectJugador
    
                Procedure OnPrompt
                    Send NavigatePath
                End_Procedure
            End_Object 

            Object oJugadorNombreCompleto is a cWebForm
                Set piColumnSpan to 5
                Set piColumnIndex to 1
                Set peLabelPosition to lpTop
                Set pbReadOnly to True
                Set pbEnabled to False
                Set psBackgroundColor to "#e8ebeb"
                
                Procedure Refresh Integer eMode
                    Forward Send Refresh eMode
                    
                    If (HasRecord(oJugador_DD(Self))) Begin
                        WebSet psValue to (Trim(jugador.Apellido) + ", " + Trim(jugador.Nombre))
                    End
                End_Procedure
            End_Object

            Object oPosicionID is a cWebForm
                Entry_Item posicion.ID
                Set piColumnIndex to 0
                Set peLabelPosition to lpTop
                Set psLabel to "Posición"
                Set pbPromptButton to True
    
                WebRegisterPath ntNavigateForward oSelectPosicion
    
                Procedure OnPrompt
                    Send NavigatePath
                End_Procedure
            End_Object 

            Object oPosicionNombre is a cWebForm
                Set piColumnSpan to 5
                Set piColumnIndex to 1
                Set peLabelPosition to lpTop
                Set pbReadOnly to True
                Set pbEnabled to False
                Set psBackgroundColor to "#e8ebeb"
                
                Procedure Refresh Integer eMode
                    Forward Send Refresh eMode
                    
                    If (HasRecord(oPosicion_DD(Self))) Begin
                        WebSet psValue to posicion.Nombre
                    End
                End_Procedure
            End_Object
        End_Object
        WebSetResponsive piColumnCount rmMobile to 4
        
    End_Object 

    Object oActionGroup is a cWebMenuGroup

        Object oSaveBtn is a cWebMenuItem
            Set psCSSClass to "WebSaveMenuItem"
            Set psCaption to "Guardar"

            Procedure OnClick
                Send Request_Save
            End_Procedure

        End_Object 

        Object oEditBtn is a cWebMenuItem
            Set psCSSClass to "WebEditMenuItem"
            Set psCaption to "Editar"
            Procedure OnClick
                Send ChangeEditMode True
                Send SetActionButtons
            End_Procedure

        End_Object 

        Object oDeleteBtn is a cWebMenuItem
            Set psCSSClass to "WebDeleteMenuItem"
            Set psCaption to "Eliminar"
            Set peActionDisplay to adMenu

            Procedure OnClick
                Send Request_Delete
            End_Procedure

        End_Object 

        Object oCancelChangesBtn is a cWebMenuItem
            Set psCSSClass to "WebIcon_Refresh"
            Set psCaption to "Deshacer cambios"
            Set peActionDisplay to adMenu
            Procedure OnClick
                Send RefreshRecord
            End_Procedure

        End_Object 
    End_Object 

    Procedure SetActionButtons
        tWebNavigateData NavigateData
        Boolean bHasRecord
        Handle hoDD

        Get Server to hoDD
        Get GetNavigateData to NavigateData
        Get HasRecord of hoDD to bHasRecord

        // let's hide all buttons and then Show the ones we want
        WebSet pbRender of oEditBtn to False
        WebSet pbRender of oSaveBtn to False
        WebSet pbRender of oCancelChangesBtn to False
        WebSet pbRender of oDeleteBtn to False

        If (NavigateData.bReadOnly) Begin
            WebSet pbRender of oEditBtn to True
        End
        Else Begin
            WebSet pbRender of oSaveBtn to True
            WebSet pbRender of oCancelChangesBtn to True
            WebSet pbRender of oDeleteBtn to bHasRecord
        End
    End_Procedure

    Procedure OnViewSaved Handle hoServer Boolean bChanged
        // Close after save
        Send NavigateClose Self
    End_Procedure

    // this will close the view after a delete
    Procedure OnViewDeleted Handle hoDDO
        Send NavigateClose Self
    End_Procedure

    Procedure OnNavigateForward tWebNavigateData NavigateData Integer hoInvokingView Integer hoInvokingObject
        Case Begin
            Case (NavigateData.eNavigateType = nfFromMain)
                // If from main, this is a propbably a main file Select to Zoom.
                Case Break

            Case (NavigateData.eNavigateType = nfFromParent)
                // If from parent, this is a constrained drill down.
                Case Break

            Case (NavigateData.eNavigateType = nfFromChild)
                // If from child, this is a probably a parent Zoom from a Zoom.
                Case Break

            Case Else // must be nfUndefined

        Case End

        Send SetActionButtons

    End_Procedure

End_Object